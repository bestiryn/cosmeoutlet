-- ============================================================================
-- Cosme Outlet — migration 002: เพิ่มคอลัมน์ "images" สำหรับแกลเลอรีรูปสินค้า
--
-- วิธีใช้: เปิด Supabase SQL Editor แล้ววางไฟล์นี้ทั้งหมด กด Run (รันครั้งเดียวพอ)
-- รันได้แม้เคยรัน schema.sql ไปแล้ว จะไม่ลบข้อมูลเดิม แค่เพิ่มคอลัมน์ใหม่
-- ============================================================================

alter table public.products
  add column if not exists images jsonb not null default '[]'::jsonb;

comment on column public.products.images is 'รูปเพิ่มเติมของสินค้า (แกลเลอรี) เรียงตามลำดับที่ต้องการแสดง ไม่รวมรูปหน้าปกใน image_url';
